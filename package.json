{
  "name": "@reidelsaltres/hellper",
  "version": "0.0.2",
  "description": "Test",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "test": "npx tsc -p tests/tsconfig.json",
    "tsc:watch": "tsc -p tsconfig.json --watch",
    "sass:watch": "sass src:out/src --watch",
    "watch": "npm-run-all --parallel tsc:watch sass:watch"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "@reidelsaltres/pureper": "^0.2.23",
    "material-symbols": "^0.43.0"
  }
}
